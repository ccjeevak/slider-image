# CSS Full Viewport Section Slider

Experimenting with full viewport slider sections, transitions, and the `clip-path` property

Steps to preview application - 
```
cd slider-image
npm install
npm run dev

```
 ![website preview](src/assets/slider-sections.gif)